import Collapses from "@/views/components/base/collapses/Collapses.vue";

const routes = [
    {
        path: "/components/base/collapses",
        name: "Collapses",
        component: Collapses,
    },
];

export default routes;
