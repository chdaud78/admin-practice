import ListGroups from "@/views/components/base/listgroups/ListGroups.vue";

const routes = [
    {
        path: "/components/base/list-groups",
        name: "ListGroups",
        component: ListGroups,
    },
];

export default routes;
