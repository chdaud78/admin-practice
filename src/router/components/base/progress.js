import Progress from "@/views/components/base/progress/Progress.vue";

const routes = [
    {
        path: "/components/base/progress",
        name: "Progress",
        component: Progress,
    },
];

export default routes;
