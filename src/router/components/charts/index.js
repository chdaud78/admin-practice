import Charts from "@/views/components/charts/Charts.vue";

const routes = [
    {
        path: "/components/charts",
        name: "Charts",
        component: Charts,
    },
];

export default routes;
