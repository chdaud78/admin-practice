import Tables from "@/views/components/tables/tables/Tables.vue";

const routes = [
    {
        path: "/components/tables/tables",
        name: "Tables",
        component: Tables,
    },
];

export default routes;
