import Widget from "@/views/components/widgets/Widget.vue";

const routes = [
    {
        path: "/components/widgets",
        name: "Widget",
        component: Widget,
    },
];

export default routes;
