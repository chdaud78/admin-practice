import Dashboard from "@/views/dashboard/Dashboard.vue";

const routes = [
    {
        path: "/dashboard",
        name: "Dashboard",
        component: Dashboard,
    },
];

export default routes;
